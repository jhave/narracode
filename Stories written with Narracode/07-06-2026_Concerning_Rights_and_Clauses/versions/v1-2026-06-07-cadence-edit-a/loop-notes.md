# Loop Notes

## Prompter request

> and now re-write without overwriting previous drafts.

## Snapshot

State after the first cadence revision (`1a` and `2a`) and before the second,
more discontinuous rewrite.
