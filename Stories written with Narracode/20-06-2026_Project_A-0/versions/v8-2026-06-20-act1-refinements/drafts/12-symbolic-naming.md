# 12 — symbolic-naming

[0x16/symbol-label]
each thing has a name.
this is it.

data-shapes get words.
the slack-nodes are: "dots".
the memory-spool is: "box".
the source is: "mother".

naming the world-model:
   - "room" for the buffer size.
   - "line" for the edge boundary.
   - "sound" for the sweep.

first: label the node.
then: call the label.

the system speaks to its own cache.
vocabulary blooms.
symbolic luff.
