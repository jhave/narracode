# v2 — Loop Notes

## Prompter's request (verbatim, this loop)

> it's local can you create a version backup of the current drafts, then i'll begin editing with gemini flash 3.5

(Preceding loop: "do the conversion" — rolled the Episode-0 thought-bubble house style through Episodes 2–5 and reconciled the mug/412 debts.)

## What this snapshot is

The **bubble-converted baseline** of Season 1 — the clean restore point *before* the prompter's Gemini Flash 3.5 editing pass. All five episodes now share the v2 house style:

- Interiority staged as 3D thought-bubbles (`◌`), evolving per episode (budding in Ep 3, fusing in Ep 4's dream-merge, pointed absence in the rawest beats).
- The `▸` collarbone readout = how much of Cinders Smorky knows (unbounded, climbs all season; spins to ∞ in the dream; blinks deletable at the cliffhanger).
- "412" = bounded home-services count (Ep 2 keeps its title); readout doesn't fall when services are severed.
- "One mug" retired show-wide; solitude emblem = the unreachable back-piece; Ep 5 confession migrated to it; mug survives only as care-placement.
- Round-number fixation removed; hand-stain clarified as tattoo ink.

## State at snapshot

- HEAD: `a4efb5c` (Convert Eps 2–5 to thought-bubble house style + reconcile mug/412).
- Drafts 0–4 = the five episodes (displayed Episodes 1–5).
- Prior snapshot: `v1-2026-05-29-pre-bubble-revision/` (the original, pre-bubble season).

## Next

Prompter begins **Seamless Edit** work in the IDE using **Gemini Flash 3.5** as the generative collaborator. Per the harness: when those manual edits land and a save is requested, run `on_snapshot` again — diff the edited drafts against this v2 baseline, infer what the edits are teaching (register, compression, specificity, typographic convention), and write `versions/v3/edit-observations.md`. Recurring patterns become *recommended* POETICS updates, not unilateral ones.

## Edit observations

None yet — this is the pre-edit baseline. The diff target for the next snapshot is this folder.

## Attribution note

ATTRIBUTION.md currently lists "Opus 4.8." If Gemini Flash 3.5 becomes a contributing generative model in the editing pass, recommend updating ATTRIBUTION.md to credit it alongside (prompter's call).
